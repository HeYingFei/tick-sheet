/**
 * 运行时配置 —— 现场可改，不必重新构建。
 *
 * 本文件由 index.html 以传统脚本同步加载；Vite 会把 public/ 下的文件原样拷贝到产物根目录，
 * 因此部署后直接编辑产物里的这个文件即可切换后端地址。
 *
 * 注意：本文件随仓库公开，请勿把真实内网地址写进默认值。
 */
window.__TICKSHEET_CONFIG__ = {
  /**
   * 后端接口地址。三种填法：
   *
   *   ''                                 按「当前页面主机名 + 8080」推导（默认，局域网直连用这个）
   *   'http://192.168.1.50:8080/api'     指向另一台机器上的后端
   *   '/api'                             同源反向代理（Nginx 把 /api 转到后端）
   */
  apiBaseUrl: ''
}
